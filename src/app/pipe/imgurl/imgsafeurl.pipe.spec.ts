import { ImgsafeurlPipe } from './imgsafeurl.pipe';

describe('ImgsafeurlPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgsafeurlPipe();
    expect(pipe).toBeTruthy();
  });
});
