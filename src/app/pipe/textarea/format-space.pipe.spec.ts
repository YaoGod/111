import { FormatSpacePipe } from './format-space.pipe';

describe('FormatSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
